# Local Supabase functions endpoint (started with `supabase start`)
# DB queries still use VITE_SUPABASE_URL (remote) so data is available.
# Only edge function calls are routed to the local instance.
VITE_FUNCTIONS_URL=http://localhost:54321/functions/v1
