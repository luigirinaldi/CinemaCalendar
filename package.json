{
  "name": "allcinemas",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "getData": "npx tsx scripts/main.ts",
    "dev": "vite",
    "build": "npm run getData && tsc && vite build",
    "preview": "vite preview",
    "format": "prettier --write .",
    "clean": "rm -rf ./dist && rm ./public/data/*"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "prettier": "^3.5.3",
    "typescript": "~5.7.2",
    "vite": "^6.3.1"
  },
  "dependencies": {
    "fullcalendar": "^6.1.17"
  }
}
